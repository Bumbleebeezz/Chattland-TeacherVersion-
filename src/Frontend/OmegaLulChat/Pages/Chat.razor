@using System.ComponentModel.DataAnnotations
@using Chattland.DataTransferContract.ChatContracts
@using Chattland.DataTransferContract.DataTransferTypes
@using Microsoft.AspNetCore.SignalR.Client
@inject IHttpClientFactory HttpClientFactory

<h3>@Room</h3>

<EditForm Model="newChatMessage" OnValidSubmit="SendMessage">
    <DataAnnotationsValidator />
    <div class="form-group">
        <label>Name</label>
        <InputText class="form-control" @bind-Value="newChatMessage.Sender.Name"></InputText>
    </div>
    <div class="form-group">
        <label>Message</label>
        <InputText class="form-control" @bind-Value="newChatMessage.Message"></InputText>
    </div>
    <button class="btn btn-primary" type="submit">Send Message</button>
</EditForm>
<hr />
<div class="col">
    <Virtualize Items="Messages.OrderByDescending(m=>m.CreatedAt).ToList()" Context="message">
        <li class="card bg-gradient border-3">
            <label class="card-header text-primary text-lg-start">@message.Sender.Name</label>
            <p class="card-body ">@message.Message</p>
            <label class="card-footer text-primary text-lg-end">@message.CreatedAt.ToString("HH:mm")</label>
        </li>
    </Virtualize>
</div>

@code {

    [Parameter]
    public string Room
    {
        get => _room;
        set
        {
            if (value.Equals(_room))
            {
                return;
            }
            _room = value;
            GetMessages();
        }
    }

    public List<ChatMessageModel> Messages = new();
    public ChatMessageModel newChatMessage = new();
    private string _room = string.Empty;

    public HubConnection hubConnection;

    protected override async Task OnInitializedAsync()
    {
        var client = HttpClientFactory.CreateClient("chatApi");
        hubConnection = new HubConnectionBuilder()
            .WithUrl(client.BaseAddress + "hubs/ChatHub")
            .Build();

        hubConnection.On<ChatMessageModel>("SendMessage", (message) =>
        {
            Messages?.Add(message);
            StateHasChanged();
        });

        await hubConnection.StartAsync(); 
        client.Dispose();
        await GetMessages();

        await base.OnInitializedAsync();
    }

    private async Task GetMessages()
    {
        Messages.Clear();
        var client = HttpClientFactory.CreateClient("chatApi");
        var response = await client.GetAsync($"/messages/{Room}?start=0&count=0");

        if (response.IsSuccessStatusCode)
        {
            var messages = await response.Content.ReadFromJsonAsync<List<ChatMessageModel>>();
            Messages.AddRange(messages);
            StateHasChanged();
        }
    }

    public class ChatMessageModel : IChatMessage
    {
        public MessageSender Sender { get; set; } = new MessageSender();
        [MinLength(1)]
        public string Message { get; set; }
        public DateTime CreatedAt { get; set; }
    }

    private async Task SendMessage(EditContext obj)
    {
        newChatMessage.CreatedAt = DateTime.UtcNow;
        await hubConnection.SendAsync("SendMessage", newChatMessage);
        newChatMessage.Message = string.Empty;
    }

}
